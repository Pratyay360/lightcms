import { type ButtonProps, type ButtonSize, type ButtonVariant, buttonVariants } from "./button";
import Root from "./button.svelte";

export {
  type ButtonProps as Props,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
  Root,
  //
  Root as Button,
};
